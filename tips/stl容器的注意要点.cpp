/*
c++的常用容器有：
    vector，pair；
    map；
    list(双向链表);
    queue/priority_queue(优先队列)/deque(双端队列);
    stack(栈);
    set(类似集合);

1. vector与list的一大区别是：
        vector内存连续但list是双链表

2. list与queue的区别主要是涉及的操作不同

*/